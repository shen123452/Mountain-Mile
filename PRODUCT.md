# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

delegated（用户委托选择）：前端 Vue 3 + Vite + TypeScript + Tailwind CSS + Three.js；**后端 Python 3.12 + FastAPI（全异步）+ SQLAlchemy 2.0 + Alembic + Pydantic v2 + OpenAI Python SDK + APScheduler**；数据库 PostgreSQL（含 pgvector）。用户明确要求使用 Vue（未用过 React）、后端使用 Python。agent 体系完整移植自参考项目 summer-checkin（MIT，原实现为 TS/AI SDK，Python 端用 OpenAI SDK function calling 对等重写），并在其基础上新增多角色协作、实时流式过程、自主档位、观测台与间隔复习能力。视觉世界参考其体素小岛并移植为框架无关的 Three.js 实现。详见 `docs/开发文档.md`。

## Users

主要用户：有长期学习需求的大学生 / 自学者（典型画像：在校大学生，同时推进多门科目或技能，如高数、英语、编程、考研准备）。

使用情境：
- 晨间让学习向导规划今日任务；白天在图书馆 / 自习室进行番茄专注，agent 在后台记录并在需要确认时暂停；
- 晚间做一次简短反思，agent 提取当日记忆、调整明日安排；
- 周日查看 agent 生成的周报与投入趋势；
- 随时通过对话让 agent 建计划、拆任务、查资料、排复习。

次要用户：查看简历项目的技术面试官——需要在演示中看到完整的 agent 循环（思考 → 工具调用 → 观察 → 再思考 → 人工确认 → 执行），并理解每一层的工程实现。

## Product Purpose

「山程」是一个 **agent 优先（agent-first）的 3D 学习成长应用**：每个学习目标都是一座从云海中升起的青绿体素山屿；一个多角色学习智能体团队（规划者 / 执行者 / 复盘者 / 记忆官 / 向导）通过工具调用真实地操作计划、任务、专注、打卡、知识库与记忆，驱动山屿生长。

产品存在的理由：
1. 学习的正反馈周期太长，我们把抽象投入即时转译成可见的地形生长；
2. 现成 AI 工具只会"回答"，不会"替你把学习运转起来"——我们要的是能观察、能规划、能执行、能反思、可被审计与人工接管的学习 agent。

成功标准：用户每天愿意打开；agent 能自主完成低风险学习事务、高风险动作可被用户审批或拒绝；一周后群岛高矮准确反映各目标投入；面试官能在演示中立刻理解 agent 机制与"地形即数据"。

## Positioning

与常见番茄钟 / 习惯打卡 / AI 聊天应用的差异机制：

1. **agent 是产品主体**：不是"带点 AI 功能的番茄钟"，而是一个有持久化运行记录、工具权限分级、人工审批、可观测可回放的学习 agent，3D 群岛是它的状态可视化；
2. **一目标一山屿**：每个学习目标拥有按 Goal ID 确定性生成的独立山屿，群岛总览即学习结构；
3. **多角色分工 + 自主档位**：规划 / 执行 / 复盘 / 记忆 / 巡逻由不同 agent 角色承担，用户可调整自主程度（全部确认 / 低风险自动 / 高自主）；
4. **过程完全可见**：agent 的思考、工具调用、观察结果通过 SSE 实时流式呈现并持久化，可回放；
5. **地形即数据**：岛屿每块地块都能追溯到一段专注、一次任务完成或一条记忆。

## Operating Context

- 学习场景：宿舍、图书馆、自习室；桌面端（笔记本）为主，移动端浏览器可查看、对话与打卡；
- 每日仪式：晨间规划 → 番茄专注（25+5，全屏沉浸）→ 晚间反思与记忆提取；
- 每周仪式：周日周报、下周建议、复习排期；
- 知识资料：可上传 md / pdf / docx / txt，agent 通过向量检索基于个人资料回答；
- 本地开发：Node 20+（前端）、Python 3.12（后端，uv 管理）、PostgreSQL 16 + pgvector；
- 生产环境：Docker Compose（Nginx 静态前端 + FastAPI/uvicorn 后端 + PostgreSQL）；定时任务由后端 APScheduler 或外部 cron 触发。

## Capabilities and Constraints

**Agent 核心（必须有，移植参考项目）**：
- Agent 运行时：Observe → Analyze/Plan → Execute 完整循环，状态机（queued/running/awaiting_approval/completed/failed/cancelled/rejected）；
- 运行持久化：AgentRun / AgentStep / AgentApproval / AgentDecision / AgentToolCall 全程记录；
- 14 个 function-calling 工具：计划（创建/查询）、任务（拆分/查询/更新/今日任务）、待办 CRUD、知识库检索、打卡与统计查询、记忆查询；
- 长期记忆：自动提取、类型/重要性/置信度、向量语义检索、去重、冷淘汰；
- 知识库 RAG：文档上传、分块、embedding、检索（pgvector）；
- 对话：多轮会话、流式回复、Markdown / 代码高亮；
- 调度：用户级 cron 配置、每日自动运行、周日周报、通知与记忆清理；
- 通知中心：每日总结、周报、提醒（去重节流）。

**差异化增强（本项目新增）**：
- 多角色 agent 协作（规划者 / 执行者 / 复盘者 / 记忆官 / 向导）与角色委派；
- SSE 实时流式 agent 过程 + 前端时间线，支持中断；
- 自主程度三档 + 工具风险分级（只读 / 低风险写入 / 高风险写入）；
- Agent 观测台：token / 成本、工具成功率、耗时、轨迹回放；
- 间隔复习工具：基于 SM-2 的复习卡自动排期（学习方向专属）；
- 新增分析工具：专注节律分析、目标达成预测、过载检测。

**二期（可选）**：MCP 外部工具协议支持、记忆关系图谱、晨昏光照与环境音。

明确不做：多人聊天室（WebSocket 群聊）、原生 App、商业化（支付/定价）。

## Brand Commitments

- 名称：山程（英文 Mountain Mile），标语"学习如登山，每一步都算数"（待最终确认）；
- 视觉世界：以北宋《千里江山图》青绿山水为文化母题——青绿山体、绢本底色、赭石与石青点缀、印章红极小面积强调；
- 字体气质：宋体（Noto Serif SC）承担标题与数字，黑体（Noto Sans SC）承担 UI；
- 语气：克制、真诚、有陪伴感；agent 解释自己的动作与理由，不假装人类、不使用夸张话术。

## Evidence on Hand

- 参考实现：`../summer-checkin`（MIT），其 agent 运行时、工具定义、记忆/RAG、调度与体素地形数学可移植，仓库 `NOTICE.md` 保留 MIT 归属；
- 文化素材：《千里江山图》（北宋·王希孟）为公共文化遗产，仅提取色彩关系，不复制画作图像；
- 无真实用户数据 / 证言 / 商业数据——演示数据全部合成并明确标注，禁止编造用户评价、成本基准或产品数据。

## Product Principles

1. **agent 的每个写动作必须可追溯、可回放、可被人工否决**——高风险动作默认停下等人确认。
2. **地形生长由真实数据驱动**——每块解锁地块都能追溯到专注、任务或记忆。
3. **先完成核心闭环，再叠加增强**——二期功能不得进入核心里程碑的关键路径。
4. **诚实的工程展示**——认证、agent 编排、记忆与 RAG 不使用不可解释的黑箱。
5. **尊重用户注意力、数据与设备**——自主程度由用户设定，尊重 reduced-motion，限制渲染成本。

## Accessibility & Inclusion

- 遵循 WCAG 2.1 AA：对比度 ≥ 4.5:1，彩色背景上不用灰字；
- 3D 与 agent 时间线提供文本替代，所有功能不依赖拖拽/鼠标也可完成；
- 键盘导航、焦点可见；流式过程可暂停阅读；
- 尊重 prefers-reduced-motion：停用岛屿浮动、呼吸与相机飞行，保留瞬时状态切换。
